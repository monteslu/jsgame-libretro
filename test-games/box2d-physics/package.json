{
  "name": "box2d-physics",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/box2d-physics.js",
  "scripts": {
    "dev": "npm run copy-wasm && vite",
    "build": "npm run copy-wasm && vite build",
    "pack": "npm run copy-wasm && vite build",
    "preview": "vite preview",
    "copy-wasm": "node copy-wasm.mjs"
  },
  "devDependencies": {
    "fflate": "^0.8.3",
    "vite": "^6.0.5"
  },
  "dependencies": {
    "box2d3-wasm": "^5.2.0"
  }
}
