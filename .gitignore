build/
deps/
node_modules/
*.log

# built game packages (rebuild from source dirs)
*.jsgame
# vite build output for npm+vite test-games (rebuild with `npm run build`)
test-games/*/dist/
build-android/
deps.*/

# Makefile.libretro drops the core in the repo root for the buildbot
/jsgame_libretro.so
/jsgame_libretro.dylib
/jsgame_libretro.dll

# compiled test harness binaries
test/harness
test/harness-gl
